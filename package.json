{
  "name": "link-previewer",
  "version": "1.0.0",
  "description": "Preview links in a popup without leaving your current page",
  "private": true,
  "scripts": {
    "build": "./build.sh chrome",
    "build:chrome": "./build.sh chrome",
    "build:firefox": "./build.sh firefox",
    "clean": "./build.sh clean"
  }
}
